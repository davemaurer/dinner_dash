
<div class="row">
  <div class="large-12 columns centered-text">
  <h1>Shopping Cart</h1>
    
    <%= button_to "Checkout", "#", class: "round button tiny" %>

    <% @cart.contents.each do |item| %>
      <%= item[0] %>: <%= item[1] %>
    <% end %>
  </div>
</div>
